import styled from 'styled-components';

const FormWrapper = styled('form')`
  grid-column-start: 1;
  grid-column-end: 8;
  display: flex;
  flex-direction: column; 
`

export const Styled = {
  FormWrapper
}